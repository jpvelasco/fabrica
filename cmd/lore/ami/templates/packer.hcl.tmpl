packer {
  required_plugins {
    amazon = {
      version = ">= 1.3.0"
      source  = "github.com/hashicorp/amazon"
    }
  }
}

variable "aws_region" {
  type        = string
  default     = "{{ .Region }}"
  description = "AWS region to build the AMI in."
}

variable "source_ami" {
  type        = string
  default     = "{{ .BaseImage }}"
  description = "Region-local Ubuntu 22.04 x86_64 base AMI ID."
}

variable "lore_version" {
  type        = string
  default     = "{{ .Version }}"
  description = "Lore server version recorded in AMI tags."
}

variable "instance_type" {
  type        = string
  default     = "m7i.xlarge"
  description = "Builder EC2 instance type. Match the runtime instance family to avoid arch mismatches."
}

variable "lore_source_dir" {
  type        = string
  description = "Local path to a directory containing the loreserver binary."
}

locals {
  ami_name = "{{ .Name }}-${formatdate("YYYYMMDD-hhmmss", timestamp())}"
}

source "amazon-ebs" "lore" {
  ami_name      = local.ami_name
  instance_type = var.instance_type
  region        = var.aws_region
  source_ami    = var.source_ami
  ssh_username  = "ubuntu"

  ami_block_device_mappings {
    device_name           = "/dev/sda1"
    volume_size           = 50
    volume_type           = "gp3"
    delete_on_termination = true
    encrypted             = true
  }

  tags = {
    ManagedBy   = "fabrica"
    LoreVersion = var.lore_version
    Name        = local.ami_name
  }
}

build {
  sources = ["source.amazon-ebs.lore"]

  provisioner "file" {
    source      = "${var.lore_source_dir}/"
    destination = "/tmp/lore-bin/"
  }

  provisioner "shell" {
    execute_command = "sudo -E bash '{{`{{ .Path }}`}}'"
    scripts         = ["${path.root}/install-lore.sh"]
  }

  provisioner "shell" {
    execute_command = "sudo -E bash '{{`{{ .Path }}`}}'"
    scripts         = ["${path.root}/verify-lore-ami-bake.sh"]
  }
}
